import db from '../database/db.init.js'

const register = async (username, email, password) => {

}

const login = async () => {
}

const logout = async () => {
}

export default {
  register,
  login,
  logout
}
